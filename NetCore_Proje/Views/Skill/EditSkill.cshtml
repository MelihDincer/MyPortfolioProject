@model EntityLayer.Concrete.Skill
@{
    ViewData["Title"] = "EditSkill";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

@*@using(Html.BeginForm("EditSkill", "Skill", FormMethod.Post)) //1. parametre action ismi, 2.parametre controller ismi, 3.parametre form method türü
{
    @Html.HiddenFor(x=> x.SkillID)
    <br />
    @Html.Label("Yetenek Başlığı")
    @Html.TextBoxFor(x=>x.Title, new {@class = "form-control"})
    <br />
    <label>Değer</label>
    @Html.TextBoxFor(x => x.Value, new { @class = "form-control" })
    <br />
    <button class="btn btn-success">Kaydet</button>
}*@
<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <form asp-action="EditSkill" asp-controller="Skill" method="post" class="form-group">
                <input type="hidden" asp-for="SkillID" />
                <br />
                <label>Yetenek Adı</label>
                <input asp-for="Title" class="form-control" />
                <br />
                <label>Değer</label>
                <input asp-for="Value" class="form-control" />
                <br />
                <button type="submit" class="btn btn-success">Güncelle</button>
            </form>
        </div>
    </div>
</div>