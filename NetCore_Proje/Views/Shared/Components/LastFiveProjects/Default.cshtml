@using EntityLayer.Concrete
@model List<Portfolio>
<div class="col-md-8 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="d-flex flex-row justify-content-between">
                <h4 class="card-title mb-1">Son 5 Proje</h4>
                <p class="text-muted mb-1">Proje Durumları</p>
            </div>
            <div class="row mt-3">
                <div class="col-12">
                    <div class="preview-list">
                        @foreach (var item in Model)
                        {
                            <div class="preview-item border-bottom">
                                <div class="preview-thumbnail">
                                    <div class="preview-icon bg-primary">
                                       @* <i class="mdi mdi-file-document"></i>*@
                                       <img src="@item.ImageUrl">
                                    </div>
                                </div>
                                <div class="preview-item-content d-sm-flex flex-grow">
                                    <div class="flex-grow">
                                        <h6 class="preview-subject">@item.Name</h6>
                                        <p class="text-muted mb-0">Ücret: @item.Price</p>
                                    </div>
                                    <div class="mr-auto text-sm-right pt-2 pt-sm-0">
                                        <p class="text-muted">Tarih: dd/MM/yyyy</p>
                                        <p class="text-muted mb-0">Tamamlanma Oranı: %@item.Value </p>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>